@{
    ViewData["Title"] = "Server Error";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <div class="card shadow-sm border-0">
                <div class="card-body py-5">
                    <h1 class="display-4 text-warning fw-bold">500</h1>
                    <p class="lead text-muted">Something went wrong on our end.</p>
                    <p class="text-muted">Please try again later or contact support if the problem persists.</p>
                    <a asp-controller="Home" asp-action="Index" class="btn btn-secondary mt-3">
                        🔄 Try Again
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>